const ms = 1000
export const seconds = (n: number) => n * ms
export const minutes = (n: number) => n * 60 * ms
export const hours = (n: number) => n * 60 * 60 * ms
export const days = (n: number) => n * 24 * 60 * 60 * ms
